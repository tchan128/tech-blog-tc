{{#each blogs as |blog| }}
<div class="blog-container">
    <div class="blog-title">
        <h1>{{blog.title}}</h1>
        <h4>Posted by {{blog.user.name}} on {{format_date blog.date_created}}</h4>
    </div>
    <div>
        <p>{{blog.content}}</p>
    </div>
</div>
{{/each}}
